files:
  "/etc/nginx/conf.d/proxy.conf":
    mode: "000644"
    owner: root
    group: root
    content: |
      location /ws/ {
      proxy_pass ws://127.0.0.1:5000/ws/;
      }

container_commands:
  nginx_reload:
     command: “sudo service nginx reload”
